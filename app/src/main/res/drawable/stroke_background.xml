<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Warna background putih -->
    <solid android:color="@android:color/white" />

    <!-- Garis pinggir merah -->
    <stroke
        android:width="2dp"
        android:color="@color/myshadow"/>

    <!-- Sudut membulat -->
    <corners android:radius="8dp" />

    <!-- Sedikit padding biar isi tidak nempel ke garis -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>
